Como rodar o código:
1 - Para poder executar o arquivo é necessario usar o comando  "flutter run -d chrome --web-renderer html" no Visual Studio Code,
2 - Também é necessário que o arquivo esteja dentro da pasta lib
